# 🛡️ PRODUCTION ENVIRONMENT CONFIGURATION
# Copy this file to .env.production and update with your actual values

# Environment
APP_ENV=production

# 🚨 CRITICAL: CORS Configuration for Production
# Replace these with your actual domain names
ALLOWED_ORIGINS=https://yourdomain.com,https://www.yourdomain.com,https://api.yourdomain.com

# Database Configuration
DATABASE_HOST=your-production-db-host
DATABASE_PORT=5432
DATABASE_USER=bruno_prod_user
DATABASE_NAME=bruno_site_prod
POSTGRES_PASSWORD=your-secure-production-password

# 🔒 Database SSL Configuration (PRODUCTION)
# 🚨 CRITICAL: Production must use SSL for database connections
# Valid modes: require, verify-ca, verify-full
# - require: SSL required (minimum for production)
# - verify-ca: SSL with CA verification (recommended)
# - verify-full: SSL with full certificate verification (most secure)
DATABASE_SSL_MODE=require

# Redis Configuration
REDIS_URL=redis://your-production-redis-host:6379

# Security Configuration
ENABLE_METRICS_AUTH=true
METRICS_USERNAME=metrics_user
METRICS_PASSWORD=your-secure-metrics-password

# Content Security Policy
ENABLE_CSP=true
CSP_POLICY=default-src 'self'; script-src 'self'; style-src 'self'; img-src 'self' https:; font-src 'self'; connect-src 'self'; frame-ancestors 'none'; base-uri 'self'; form-action 'self';

# LLM Configuration
GEMMA_MODEL=gemma3n:e4b
OLLAMA_URL=http://192.168.0.3:11434

# Application Configuration
PORT=8080
GIN_MODE=release
